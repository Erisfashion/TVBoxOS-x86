name: Build TVBox Android 4.2 x86 APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '11'

      # 关键步骤：生成 local.properties 注入 SDK 路径，杜绝 FileNotFound 异常
      - name: Create local.properties
        run: |
          touch local.properties
          echo "sdk.dir=$ANDROID_HOME" > local.properties

      - name: Grant Execute Permission for Gradlew
        run: chmod +x gradlew

      # 核心修改：加上 --refresh-dependencies 强制刷新依赖缓存并清理旧构建
      - name: Build Debug APK
        run: ./gradlew clean :app:assembleDebug --stacktrace --info --warning-mode all

      - name: Upload Output APK
        uses: actions/upload-artifact@v4
        with:
          name: TVBoxOS-Android4.2.2-x86-Tablet
          path: app/build/outputs/apk/**/*.apk
          retention-days: 7
